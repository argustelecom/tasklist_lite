name: tasklist_lite
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  google_fonts: ^2.0.0
  # pure google_fonts не поддерживают кириллицу у многих шрифтов
  # просто отображает в дефолтном Roboto. Пришлось подключить это.
  google_language_fonts: ^2.0.0
  adaptive_breakpoints: ^0.1.0
  get: any

  carousel_slider: ^4.0.0
  graphql_flutter: ^5.0.0

  intl: ^0.17.0
  substring_highlight: ^1.0.33
  flutter_html: ^2.2.1


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

  duration: ^3.0.8

  shared_preferences: ^2.0.12

  flutter_typeahead: ^3.2.4

  dropdown_button2: ^1.1.1
  url_launcher: ^6.0.0

  flutter_secure_storage: ^5.0.2
  package_info_plus: ^1.3.1
  flutter_dotenv: ^5.0.2

  # работа с файлами
  file_picker: ^4.4.0
  image_picker: ^0.8.4+9
  # утилита, расширяющая возможности iterable (например, добавляет distinct в List и т.д.)
  darq: ^1.1.1
  # открытие файла по пути
  open_file: ^3.2.1
  # поиск путей в рамках ОС
  path_provider: ^2.0.9
  filesystem_picker: ^2.0.0
  # удобный плагин для работы с пермишенами на мобилке
  permission_handler: ^9.2.0

  percent_indicator: ^4.0.0
  logging: ^1.0.2


dev_dependencies:
  # для unit тестов
  test: ^1.17.0
  # для интеграционных тестов
  integration_test:
    sdk: flutter
  # для widget тестов
  flutter_test:
    sdk: flutter
  # для mock
  mockito: ^5.0.17
  # для запуска генератора кода: https://docs.flutter.dev/cookbook/testing/unit/mocking
  build_runner: ^2.1.7

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/logo_figaro.png
    - images/ibob_backpack_icon.png
    - config/app.env
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
